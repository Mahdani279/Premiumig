# Premiumig

 $ pkg update && upgrade
    $ pkg install git
    $ pkg install python
    $ pip install requests
    $ pip install futures
    $ git clone https://github.com/kenDevXD/Premiumig
    $ cd Premium
    $ git pull
    $ python Zpedia.py
